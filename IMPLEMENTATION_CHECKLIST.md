# Checklist: การแก้ไขระบบลืมรหัสผ่าน ✅

## ✅ การแก้ไขในโค้ด (เสร็จแล้ว)

- [x] ปรับปรุง `forgot_screen.dart` - เพิ่ม validation และ async handling
- [x] สร้าง `reset_password_screen.dart` - หน้าจออนุญาตให้ผู้ใช้ตั้งรหัสผ่านใหม่
- [x] ปรับปรุง `auth_provider.dart` - เปลี่ยน redirect URL และเพิ่มฟังก์ชัน updatePassword
- [x] ปรับปรุง `app_router.dart` - เพิ่ม route สำหรับหน้า reset password
- [x] ปรับปรุง `AndroidManifest.xml` - ตั้งค่า deep link intent-filter
- [x] ปรับปรุง `Info.plist` - ตั้งค่า URL scheme สำหรับ iOS
- [x] ตรวจสอบว่าโค้ดไม่มี error - `flutter analyze` ✓

## 📋 ขั้นตอนที่ต้องทำต่อไป (จำเป็น)

### ⚠️ CRITICAL: ตั้งค่า Supabase Dashboard

1. **เข้า Supabase Console**:
   - ไปที่ https://app.supabase.com
   - เลือกโปรเจกต์ของคุณ: `vdhaeocpcxiefuzetikh`

2. **ไปที่ Authentication → URL Configuration**

3. **อัปเดต Redirect URLs**:
   - เพิ่ม URL ใหม่:
     ```
     io.supabase.muta://reset-password
     io.supabase.muta://reset-password/*
     ```
   - กดปุ่ม "Save changes"

4. **ตรวจสอบว่าบันทึกสำเร็จ**:
   - ควรมี checkmark หรือ success message

---

## 🧪 การทดสอบ

### ขั้นตอนที่ 1: เตรียมแอป
```bash
cd c:\Users\thiti\projects\muta
flutter clean
flutter pub get
flutter run
```

### ขั้นตอนที่ 2: ทดสอบหน้าลืมรหัสผ่าน
1. ไปที่หน้า "ลืมรหัสผ่าน"
2. ใส่อีเมล (ตัวอย่าง: `test@example.com`)
3. คลิก "ส่งลิงก์"
4. ตรวจสอบข้อความเตือน

### ขั้นตอนที่ 3: ทดสอบอีเมล (หากใช้ Supabase Production)
1. ตรวจสอบ mailbox
2. หาอีเมลจาก Supabase (อาจไปยัง Spam)
3. คลิกลิงก์ "Reset Password"

### ขั้นตอนที่ 4: ทดสอบหน้า Reset Password
1. ลิงก์ควรเปลี่ยนเส้นทางมาที่แอป
2. แอปควรแสดง "ตั้งรหัสผ่านใหม่"
3. ใส่รหัสผ่านใหม่ 2 ครั้ง
4. คลิก "อัปเดตรหัสผ่าน"
5. ถ้าสำเร็จจะกลับไปหน้า login

### ขั้นตอนที่ 5: ทดสอบล็อกอินด้วยรหัสผ่านใหม่
1. ลองเข้าสู่ระบบด้วยรหัสผ่านใหม่
2. ถ้าสำเร็จแสดงว่าระบบทำงานได้

---

## 🔧 การแก้ไขปัญหา

### ปัญหา: ส่งลิงก์ แต่ไม่ได้รับอีเมล
**วิธีแก้**:
1. ตรวจสอบ Supabase > Auth > Users มีผู้ใช้อยู่หรือไม่
2. ตรวจสอบ Spam folder
3. ตรวจสอบ Email settings ใน Supabase Dashboard

### ปัญหา: คลิกลิงก์ แต่ยังไม่ไปที่แอป
**วิธีแก้**:
1. ✅ ตรวจสอบว่า Redirect URLs ตั้งค่าใน Supabase แล้ว
2. ✅ ตรวจสอบ AndroidManifest.xml มี intent-filter ถูกต้อง
3. ✅ ตรวจสอบ Info.plist มี URL scheme ถูกต้อง
4. ลองเรียกใช้แอปใหม่: `flutter clean && flutter run`

### ปัญหา: "No Activity found to handle Intent" (Android)
**วิธีแก้**:
1. ตรวจสอบ deep link scheme ตรงกันหมด: `io.supabase.muta`
2. ตรวจสอบ AndroidManifest.xml มี `android:exported="true"`
3. ลองรัน: `adb shell am start -a android.intent.action.VIEW -d "io.supabase.muta://reset-password" com.example.muta`

### ปัญหา: "Invalid redirect URL" จาก Supabase
**วิธีแก้**:
1. ตรวจสอบ URL ไม่มี space หรือ typo
2. ตรวจสอบ URL scheme ถูกต้อง: `io.supabase.muta://reset-password`
3. ลองเพิ่มทั้ง URL แบบ exact และ wildcard:
   - `io.supabase.muta://reset-password`
   - `io.supabase.muta://reset-password/*`

---

## 📝 ไฟล์ที่เปลี่ยนแปลง

```
lib/
├── src/
│   ├── providers/
│   │   └── auth_provider.dart ✏️ (เปลี่ยนแปลง)
│   ├── screen/
│   │   ├── forgot_screen.dart ✏️ (เปลี่ยนแปลง)
│   │   └── reset_password_screen.dart ✨ (ใหม่)
│   └── routes/
│       └── app_router.dart ✏️ (เปลี่ยนแปลง)
├── main.dart (ไม่ต้องเปลี่ยน)
│
android/
└── app/src/main/
    └── AndroidManifest.xml ✏️ (เปลี่ยนแปลง)

ios/
└── Runner/
    └── Info.plist ✏️ (เปลี่ยนแปลง)
```

---

## 📚 เอกสารเพิ่มเติม

อ่านหาวิธีแก้ไขปัญหาเพิ่มเติมในไฟล์:
- `PASSWORD_RESET_FIX_SUMMARY.md` - สรุปทั้งหมด
- `SUPABASE_REDIRECT_URL_SETUP.md` - ขั้นตอนตั้งค่า Supabase
- `DEEP_LINK_TESTING_GUIDE.md` - วิธีทดสอบ Deep Link

---

## ✨ สิ่งที่คุณจะได้

หลังจากทำตามขั้นตอนทั้งหมด:

✅ ผู้ใช้สามารถใส่อีเมลเพื่อรีเซ็ตรหัสผ่าน
✅ Supabase ส่งอีเมลพร้อมลิงก์
✅ ผู้ใช้คลิกลิงก์ → แอปเปิดโดยอัตโนมัติ
✅ แสดงหน้าตั้งรหัสผ่านใหม่
✅ ผู้ใช้สามารถตั้งรหัสผ่านใหม่ได้
✅ ล็อกอินด้วยรหัสผ่านใหม่ได้

---

## ❓ คำถามหรือปัญหา?

1. ตรวจสอบเอกสารเพิ่มเติมที่มีอยู่
2. ตรวจสอบ Supabase Dashboard เทือก
3. ลองรัน `flutter clean` และ `flutter run` ใหม่
